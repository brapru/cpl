#include "cpl.h"

/*
 * max, min
 *
 * if min > max:
 * 	return NOTFOUND;
 *
 * get_midpoint
 * 
 * if get_midpoint = key ?
 *	return midpoint;
 *
 * if get_midpoint < key
 * 	binsearch(get_midpoint + 1)
 * 
 * if get_midpoint > key
 * 	binsearch(get_midpoint - 1)
 *
 */

static inline void binsearch(void){
	printf("BSEARCH\n");
}
